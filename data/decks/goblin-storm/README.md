# goblin-storm

Awaiting the locked decklist. Paste it as `decklist.txt` in this directory:

```
Commander:
1 Wort, Boggart Auntie

1 Skirk Prospector
1 Empty the Warrens
...
```

Formats accepted: `1 Card Name`, `1x Card Name`, bare names, a `Commander:`
section header or trailing `*CMDR*` marker, `#`/`//` comments.

Then run:

```
manamap pilot fetch-deck goblin-storm
manamap pilot validate-deck goblin-storm
```
