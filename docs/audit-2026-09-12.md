# Audit 2026-09-12 — the inventory before grooming

**What this is.** A read-only inventory of the repo at `bccca716`, taken before a
backlog-grooming and debt-paydown pass. Nothing here is a fix; every row is a
fact with a location, so the grooming can be argued from evidence rather than
memory. It follows `docs/agent-audit-2026-08-19.md` in spirit and should be
moved to `docs/history/` once the work it names is done or filed.

**How it was made.** Seven parallel read-only reviews (docs, the pilot package,
the rest of `src/`, `viz/`, the git history, the 48 GitHub issues, the test
suite), a fleet listing from `data/decks/*/deck_versions.json`, and one fresh
`make test` on this checkout. Figures are as measured on 2026-09-12; where a doc
states a different figure, both are given.

---

## 1. What the thing is, by the numbers

| | measured | what the docs say |
|---|---|---|
| Commits | 841 (Apr 5 · Jul 158 · Aug 386 · Sep 292 in 12 days) | — |
| Python, `src/manamap/` | 66,540 LOC; `pilot/` 44,116 in 107 modules | — |
| Legacy magazine renderer | ~6,600 LOC (15% of pilot), 8 modules + `build_page.py` | "frozen" |
| Frontend `viz/` | 18,062 lines; `mana-map.js` 3,354 | "nine busts on index.html" (eleven) |
| Pages | **5** (`spaces.html` since 2026-09-01) | CLAUDE.md "FOUR pages"; vision.md "two surfaces" |
| Top-level CLI subcommands | **28** | CLAUDE.md:205 "18" |
| Pilot subcommands | 107 | 107 (verifies); `console.py`/`serve.py`/`cli.py` say 66/69/102 |
| Tests | 180 files; 3,717 default / 3,971 with `-m ""`; 249 browser | testing.md "119 files, 2,882 / 227 browser" |
| Red on `main` | **13 failed / 3,371 passed / 327 skipped** | known-issues "9 failing" (09-08) |
| Agents / skills | 17 / 21 | agent-inventory 17/21 verifies; README:48 "15" |
| Decks | 12 dirs; 5 sleeved · 2 bench · 4 broken-down · 1 retired | PLAN.md table lists 10 |
| Branch dirs | 28 (3 PROPOSED, 1 merged) | — |
| Games logged / annotated | 15 / 15 | known-issues §9 "11 / 0"; PLAN "8 games, 6 undebriefed" |
| Opponent seats / pods | 14 / 7 | PLAN "eleven seats, five pods" |
| Tracked `data/` | 234 MB in 678 files; `.git` 217 MB; `data/` on disk 1.3 GB | — |
| Mixed code+artifact commits | 255 of 841 (30%); 37% in August | — |
| `make test` wall | **319 s warm** (318 cache hits), `-n auto` | Makefile "~20s"; testing.md "~2.5 min" and "735 s"; CLAUDE.md "772 s" |

---

## 2. The fleet

| slug | lifecycle | paper | branches | sims | logged | notes |
|---|---|---|---|---|---|---|
| edgar-vampires | active, SLEEVED | v2 (V2) | 6 | 9 | 4 | five single-card patch branches PLAN says "decide or delete"; `elenda-v1` staged 09-12 |
| gishath | active, SLEEVED | v4 | 2 | 3 | 1 | `mana-v1` PROPOSED as v1.1.0 |
| goblin-storm | active, SLEEVED | v1 | 0 | 4 | 2 | 0.031 at standard-v3; 34 DARK cards (#33) |
| heliod | active, SLEEVED | v10 | 2 | 4 | 2 | `splendor-v1` PROPOSED as v1.2.2; `archangel-v1` merged with `into_version_before: null` |
| ur-dragon | active, SLEEVED | v4 | 6 | 4 | 6 | `final-v1` PROPOSED as v1.3.0; engine.json FAILS validate (Shivan Reef) |
| sharknado | bench | — | 1 | 0 | 0 | proposals withdrawn 09-11 |
| ingris-infect | bench | — | 3 | 0 | 0 | **no `deck_versions.json`** — the only deck without one; legal 2026-10-02 |
| zur-enchantress | **broken-down 2026-09-10** | — | **8** | 2 | 0 | "failed experiment"; 8 branch dirs on a deck that is apart; 15 versions |
| hapatra | broken-down 2026-09-08 | — | 0 | 1 | 0 | parts sleeved in yawgmoth-swarm; still parametrised in `test_pilot_goldfish_mana.py:81` |
| yawgmoth-swarm | broken-down | — | 0 | 1 | 0 | 37 DARK cards |
| radagast | broken-down 2026-08-21 | — | 0 | 3 | 0 | hard-coded in 15 test files (53 uses) |
| sisay | retired | — | 0 | 1 | 0 | a friend's deck; never revive |

Every deck has both `versions.json` (derived from git) and `deck_versions.json`
(authored) except ingris-infect, which has only the derived one.

---

## 3. The red board today

`docs/known-issues.md` was last verified 2026-09-08 at 9 failing / 3,217
passing / 208 skipped. This is a fresh run on `bccca716`.

**Measured on this checkout, 2026-09-12: 13 failed · 3,371 passed · 327 skipped
(318 of them cache hits) · 6 xfailed · 319 s warm on `-n auto`.** So the count
has moved 9 → 13 since known-issues was last verified, and the suite has grown
by ~450 cases since its figure was taken.

| failing test | cause | tracked as |
|---|---|---|
| `artifact_freshness::versions_json_matches_a_fresh_run[ur-dragon]` | versions.json stale | known-issues §1 |
| `tracked_artifacts_validate[ur-dragon/engine.json]` | engine names Shivan Reef / Stormcarved Coast | §1, #15, #36 |
| `agent_stamps::no_agent_artifact_names_a_card_the_deck_does_not_run` | same cause, fleet-scoped | §1, #36 |
| `poh::a_procedure_page_names_only_cards_the_deck_runs` | ur-dragon procedures name Shivan Reef | §1, #13 |
| `poh::a_rebuild_is_byte_identical` | same cluster | §1 |
| `goldfish_drain_and_draw::the_runaway_guard_holds` | Edgar's four free outlets under a token-minting commander | §3a, #34 |
| `goldfish_colors::a_mono_colour_deck_is_barely_affected…` | five-colour deck did not move: [0.219, 0.319] | §3c, #35 |
| `deck_info::a_real_deck_composes_every_panel` | heliod `engine.critic == "fail"` | §2, #15 |
| **`tracked_artifacts_validate[heliod/goldfish_targets.json]`** | **"Greater Auramancy is declared in a target but is not in the deck — a swap stranded the name"** | **#47, and it is red on a SLEEVED deck as of the 09-12 staging** |
| **`artifact_freshness::info_json_matches_a_fresh_run[edgar-vampires]`** | info.json stale after `elenda-v1` was staged 09-12 | new |
| **`sim_pilot_quality::no_tracked_run_is_flagged_by_accident`** | the piloting gate fires at 0.826 on a tracked Edgar run (`sythis-…-n40-cdaddf26-s341395706-podExperimental-c600.json`) | new — decide true positive or `KNOWN_FLAGGED` |
| **`viz_ladder::the_gate_count_matches_what_promote_would_print`** | `assert 2 >= 3` — info.json's gate count and `promote.stage` disagree | new, adjacent to #42 |
| **`serve_cli::the_client_fails_open_when_no_server_is_listening`** | the test's own comment says "nothing on :1" but it never sets `MANAMAP_DAEMON`, so it hits the default `127.0.0.1:8000` and gets a real answer (`manamap serve` is running here). **It fails for anyone following the project's own recommended warm-worker workflow.** | new, a test bug |

The last five rows are not on any board. Four are consequences of the 09-11/09-12
staging and proposal work, which is the pattern issue #45's cluster describes:
a branch write lands and the champion's artifacts go stale or strand a name,
with nothing noticing until the suite runs.

Rows in `known-issues.md` whose status has moved since 09-08:

- **§9 (11 logged, 0 debriefed) — resolved.** 15 entries, every one has a
  `log_annotations.json` row (files dated 09-11). GitHub #39's debrief half is
  done; its Edgar-direction half is not.
- **§13 (heliod Forge figures describe v1.0.0) — resolved.** v1.2.1 runs exist
  and §4 already quotes one.
- **§4 (parser drops 88% of noncombat damage)** — header says unfixed, body at
  :261 says the owner map is fixed (`parse.py:257-285`); the naming split is
  what remains. Header needs the update.
- **§5** lists zur-enchantress as live; it was broken down 09-10.
- **§1** cause has moved: ur-dragon's paper is now locked (v1.2.1), so the
  unblocker is `/analyze-engine`, not a check-in.

---

## 4. GitHub issues — 42 open, 4 closed

### Close now (fixed in code, still open on GitHub)

| # | title | evidence |
|---|---|---|
| 27 | SEEDED_NOTE claims byte-for-byte replay | rewritten "SEEDED FIXES THE SHUFFLE, NOT THE GAMES", `forge.py:67` (9b3717b2) |
| 42 | deck_info counted gate rows in its denominator | `test_pilot_deck_info.py:277,307` — but see the new `viz_ladder` failure in §3, which is the same disagreement on a different surface; check it before closing |
| 43 | CI depth-1 checkout | `fetch-depth: 0` at `test.yml:56,120` |

### Themes, with the one issue that unblocks the rest

| theme | issues | unblocker | notes |
|---|---|---|---|
| A. Branch / propose lifecycle | #45 #46 #47 #48 #25 | **#45** (a branched measurement must carry the branch's sha, on disk) | filed 09-12; #47's read path already prefers a branch-local `goldfish_targets.json` (`common.deck_file:582`), nothing writes one |
| B. Agent-artifact drift | #36 #15 #13 #44 #20 #41 #39 | **#44's second half** (merge steps write `decklist_sha256`) + #20's model stamp | #13 is superseded by the POH; #41 (workbench blurb) must not ship before the stamp or it is a fifth stale artifact |
| C. Goldfish / audit fidelity | #34 #35 #33 #17 #19 #21 | **#34 / #35** are real `model_*` bugs on sleeved decks | #33 is per-deck judgement (gishath now DARK 0; goblin-storm 34) |
| D. Forge scheduler / stats | #26 #32 (#27 done) | **#26 point 3** (one JVM per game) | #32's overflow is above n≈1000, a 400-game arm is close |
| E. Tests & CI | #28 #31 #16 #48.2 (#42 #43 done) | **#28** (cache keys on all of `src/`) | this is the daily-loop cost; see §5.J |
| F. Wall-clock parallelism | #29 #30 | independent | `candidates`/`card-value` run 10k games per card serially |
| G. Embedding research | #12 → #40 → #38 | strictly in that order | #38's names key on signatures a retrain invalidates |
| H. PRD / product surfaces | #6 → #8 → #7; #9; #10; #11 ↔ #14; #5; #4 | **#6** for the benchmark trio; **#11** for #14 | #6–#14's §/Phase refs resolve against the **superseded** `prd-2026-08.md` |
| I. Robustness | #23 (+#21) | — | 21 unreasoned `except Exception` in pilot, 6 outside |

Duplicates / pairs: #11 and #14 are one design; #7 and #10 overlap on fleet
aggregates; #26 and #27 a pair; #15/#36/#13/#44 one cluster.

### Named follow-ups that have no issue

- known-issues §4 (parser naming split), §8 (50 engine `open_questions`, no
  queue), §12 (`experiment` does not rotate seats — `experiment.py:416-417`
  still `Ai(1)`), O1–O7 (net_change has no model/champion stamp; aborted runs
  returned by `list_runs`; rounding-boundary target jumps).
- PLAN: `token_bodies` reads the whole text (nine fleet cards credited a free
  body); Edgar's five patch branches "decide or delete"; radagast/sisay
  unpinned so `regen --slug` silently matches nothing.
- Commit bodies: `captains-log` printer raises `KeyError 'ship'` (ce6456e2);
  PRD §14.1 aggregation "not yet" (b63da65c); "the deferred deploy-time step
  with git available was never built" (a1b0ecec); Sven "has not yet said
  anything a model wrote" (9f661d68).

---

## 5. Tech debt, by area

Each row: the fact, where, a size (S = an hour, M = a session, L = days).

### A. The legacy magazine renderer (issue #4) — L

- 8 self-labelled LEGACY modules + `build_page.py` = ~6,600 LOC. `poh.py`
  imports none of them.
- **The only live-code edge** is `build_index.py:22-24` ← `design.{stylesheet_link,
  write_stylesheet, FONT_LINK, badge, barcode, esc}` + `issue_spec`. And
  `build_index.line_cards` is imported by `engine_facts.py:33`, `deck_map.py:55`,
  `validate_engine.py:86`, `serve.py:428,1088`, `deck_delete.py:148`,
  `deck_state.py:96`. Split `line_cards` and the JSON manifest out of
  `build_index.py` and the graph is severed.
- CLAUDE.md says `issue_length` is imported by live code; it is not (only
  `build_page.py:575`).
- Stylesheet plumbing is triplicated: `design.py`, `page_design.py`,
  `poh_design.py` each carry `stylesheet_version` / `write_stylesheet`.
- `page_spec.py` is live-by-test (`test_viz_deck_lens.py:242` locks
  `deck.html` to `DOSSIER_SECTIONS`); `page_design.py` is legacy-by-use.
- Outside src: `Makefile:96` (`make manuals` runs `build-manual`, and CI diffs
  `manuals/` — the determinism gate exercises the frozen renderer), three
  skills, `pilot-notes.md:53,72`, and 13 test files importing legacy modules
  (`test_pilot_build_manual.py` 32 imports, `test_pilot_validate_issue.py` 19).
- `registry.py:12` still describes `build-page` as "the compact deck page (the
  Pilot's Manual)".

### B. `goldfish.py` — L, but the proof is cheap

- 5,830 lines. `simulate_once` is **1,999 lines**, 239 assigned locals, 10
  nested closures, a 33-key return dict; `run` is 480 lines with 17 params.
- 12 `model_*` flags, threaded into `simulate_once` as 26 keyword params.
  `test_metric_hygiene` covers `classify()`'s emitted keys only, not the flags;
  `model_coverage.CHANNELS` (:45) lists 7 of them and omits
  `model_commander_animate`, `model_commander_combat_reveal`, `model_deaths` —
  a new commander flag can be declared, read, and invisible to coverage.
- `run` raises `SystemExit` at :5229, :5308, :5359, :5380, :5450 and is called
  in-process by `benchmark.py:213`, `diagnostic.py:488`, `deck_branch.py:497`,
  `calibrate.py:164` — a declaration typo kills the caller mid-regen.
- Candidate split with no behaviour change: `profiles` (~lines 300–2740, pure
  regex classifiers), `assumptions` (:76-305), `declaration` (:5293-5450),
  `turn` (the closures become methods on a state object; the comment-marked
  phases at :3233/:3284/:3545/:3698/:4139/:4409/:4887 become functions),
  `aggregate` (:4941-5175, :5690-5830). Seeded determinism holds if the `rng`
  call order is untouched; a byte-identical `goldfish_metrics.json` across the
  fleet is the proof.

### C. Predicate sprawl — M each, S for some

- **"The current decklist sha" has two meanings and six definitions.** Hash of
  `decklist.txt`: `deck_notes.decklist_sha256:87` (`read_bytes`),
  `deck_versions.working_sha:367` (`read_text`), `deck_branch._sha_of_list:865`,
  `check_in.py:177`, `deck_branch.py:728,1196`, `fetch_deck.py:403`. Stamp on
  `cards.json`: `install_agent.decklist_sha:78`, `deck_info._current_sha:534`,
  `validate_diagnosis.is_stale:569`. Bytes-vs-text means a CRLF list hashes
  differently in two modules.
- **Staleness compare two ways**: `deck_status._stamp_is_stale:160` is
  prefix-tolerant ("three decks store twelve characters");
  `validate_diagnosis.is_stale:569` is exact `!=` — the bug the former fixed.
- **`common.expand_copies` exists; 24 hand-rolled `get("quantity")` sums
  remain** (`deck_facts.py:349,353`, `poh.py:114`, `deck_history.py:95`,
  `check_in.py:54`, `mana_analysis.py:187,195`, `pool_facts.py:134`,
  `deck_branch.py:549,624,682,689`, `validate_deck.py:21,41` — the last defaults
  to 0, every other to 1).
- **"What artifacts does a deck have / need" lives in five registries**:
  `deck_status.STAGES/VALIDATED` (:43/:318), `regen.STAGES/BOOTSTRAP`
  (:51/:100), `promote.GATES:63`, `assess._GATES:45`, `deck_info._gates:650`.
  `serve.MEASURES/SCAFFOLDS` (:797-819) is a sixth.
- Lifecycle: `common.deck_is_apart:523` is the home, but `deck_info.STATE_RETIRED:813`,
  `regen.is_retired:63`, `deck_versions.paper_state:181` still re-read
  `UNPLAYABLE_STATUSES`, and `deck_branch._deck_holders:188` calls both readers.
- Agent handoff filename declared per module: `install_agent.AGENT_FILE:48`,
  `merge_prose:27`, `merge_debrief:16`, `merge_deck_map:22`,
  `merge_captains_log:21`, `prescribe:35`.
- `common.load_json` exists; 62 direct `json.load(s)` bypass it.
- Frontend twins: `getJSON` byte-identical in `deck-view.js:109`,
  `workbench.js:50`, `branch-view.js:26` (a fourth, different, in
  `build.js:136`); `esc` ×4 plus `MM.escHtml` which drops `'`; `num` ×3
  different; `versionName` ×2 with different semantics; `cardImageUrl`
  reimplemented with a different default size.

### D. Error handling and control flow — M

- `SystemExit` raised **259** times in pilot (~60 inside non-`main` library
  functions: `deck_branch.stage/unstage/commit/propose`,
  `deck_versions.tag/set_paper`, `common.resolve_out_path:314`,
  `common.report_errors:429`) and 67 times outside pilot (`forge.py` ×11,
  `experiment.py` ×8, `bridge.py` ×6).
- `except Exception`: 51 in pilot, **21 with no reason on the line**
  (`deck_info.py:251,256,604,619`, `calibrate.py:115,128,170`, `assess.py:178`,
  `autobuild.py:566`, `build_deck.py:435`, `deck_history.py:218`,
  `deck_status.py:437`, `impact.py:180`, `net_change.py:639`, `promote.py:188`,
  `scenario_facts.py:94`, `validate_stack.py:204`, `goldfish.py:5517`,
  `validate_deck.py:83`, `validate_goldfish_targets.py:301`, `upgrades.py:165`);
  `serve.py:707` `except BaseException` in a job thread; `forge.py:1178` silent.
  Bare `except:` — zero. Issue #23 names three of these.
- `logging` is used nowhere. `print` ×641 in pilot, ~330 elsewhere;
  `console.say/err` only in newer modules.
- Module-level mutable caches: `common` ×4 (`_JSON_MEMO:56`, `_MTIME_MEMO:72`,
  `_RULES_DB_MEMO:612`, `_STRATEGY_SHA_MEMO:666`), `agent_cache._SHA_MEMO:64`
  (never evicts — known-issues D7), `collection._COLLECTION_MEMO:45`,
  `goldfish._CREATURE_TYPES_CACHE:2607`, `build_manual.CARD_ANCHORS:234`
  (mutated during render), `serve.JOBS:482` (unbounded), `serve._SVEN_SESSION:963`,
  `preprocess._MODEL_CACHE:47`, `bridge._CR_TO_FORGE:64`, `console._RICH:52`.
- TODO/FIXME/HACK markers: **none** anywhere. Debt lives in commit bodies,
  PLAN.md and known-issues.md instead.

### E. Functions over 150 lines — 16 in pilot, 2 in sim

`goldfish.simulate_once` 1,999 · `registry.add_pilot_parser` **754** (74 `if
name ==` branches, :222) · `goldfish.run` 480 · `parse.game_facts` 332 (:346) ·
`goldfish.combat_profile` 291 · `parse.aggregate` 253 (:1003) ·
`design.deck_constellation` 247 · `deck_branch.main` 236 (:1294) ·
`goldfish.aggregate` 228 · `deck_info.compose` 214 (:318) ·
`build_index.gather_entries` 197 (:217) · `mana_analysis.analyze` 182 (:149) ·
`validate_engine.validate` 175 (:141) · `net_change._print` 173 (:1058) ·
`deck_branch.merge` 163 (:1069) · `diagnosis_report.render` 160 (:51) ·
`build_deck.build` 156 (:754) · `design.engine_flow` 150.

### F. `serve.py` — M

- `_build_finish` runs `git add` / `git commit` from an HTTP handler
  (:452-458).
- `AGENT_COSTS` (:487) hand-copied from `docs/agent-cost.md`.
- The `ask` bridge (`_spawn:497-516`) passes `--agents` with only a
  `description`, which *defines* an inline agent rather than resolving
  `.claude/agents/<name>.md` as the comment at :500-502 claims; `test_serve.py`
  mocks the subprocess so this is unverified.
- All warm `/api/cli` calls serialise on `_STDOUT_LOCK` (:955) because output is
  captured by `redirect_stdout`.
- Two argparse shims (`_Args:781`, `type("A",(),{})()` at :437).
- `mcp_server.py` is clean (183 lines); protocol version hand-written (:39).

### G. Experiments left in the tree — M to remove, or S to document

- VAE + CardBERT: ~3,600 LOC across `training/{card_fields,card_parse,
  card_serialize,card_source,loss_cardbert,masking,model_cardbert,model_vae,
  recoverability,span_encoder,train_cardbert,train_vae,vae_cache}`. "Abandoned
  by its own control" (1328d648); CardBERT survives as a toggle. VICReg built,
  tested, never run (#40).
- **Four tracked shadow artifacts** (`embeddings_cardbert.bin` 17 MB,
  `projection_2d_cardbert.json` 13 MB, `regions_cardbert.json`,
  `neighbours_cardbert.bin`) absent from `docs/data-artifacts.md`; regions
  unnamed (#38: 0 names vs 192/87).
- Six CLI commands no doc mentions: `train-vae`, `span-cache`, `recoverability`,
  `train-cardbert`, `eval-commander-search`, `ask`.
- `recoverability.py` and `vae_cache.py` have no tests; `project_spaces` untested.

### H. `config.py` — S

- 225 constants, 1,600 lines, zero functions. Not config: `AGENT_ROUTINES`
  (12 entries, ~175 lines with prose citations), `DECK_AXIS_TARGETS` (~130
  lines), `PROSE_KEY_INPUTS`, `RESOLVE_SCOPE_BUDGET`.
- **Unused anywhere**: `COLOR_FEATURE_DIM`, `MANA_PIPS_DIM`,
  `POWER_TOUGHNESS_DIM` (only summed at :282), `DECK_AUDIT_PATH`,
  `POH_SPEC_PATH`, `EVAL_DIR`, `STRATEGY_DIR`.
- Loop limits (`DECK_BUILD_MAX_ITERATIONS`, `DIAGNOSE_MAX_ITERATIONS`,
  `ENGINE_MAX_ITERATIONS`, `STRATEGY_RESEARCH_MAX_ITERATIONS`) are read by no
  code — enforced by skill prose only.
- `MECHANICAL_TAGS` 33, `ROLE_PATTERNS` 40 → 53 names, `SYNERGY_RULES` 24.

### I. Frontend — M

- `mana-map.js` is the god-module and **growing**: 1,795 at the canvas port →
  2,603 (08-24) → 3,354. `session.js` and `shell.js` were additive, not
  extractive.
- `window.MM` exports 61 names. Never read outside the file: `buildHoverTextMinimal`
  (self-described "uncalled by design", :214), `cardImageUrl`, `getSynergyGraph`,
  `groupColour`, `MAP_CONFIGS`, `obsolescence` (its consumer is gone),
  `orientTo`, `renderManaSymbols`. Eleven more are read only by tests.
  `docs/viz.md:1076` claims every member has a live caller.
- Dead: `mana-map.js:203 getSelectedCard`, `discovery.js:579 relBtn`; the
  `histogram2dcontour` trace is still *built* (`mana-map.js:1898`) only to be
  filtered and warned about (`canvas.js:154-158`).
- **Three cache-bust constants for one file** `decks/index.json`: `?v=3`
  (`workbench.js:33`), `?v=2` (`build.js:47`), `?v=9` via `DATA_VERSION`
  (`discovery.js:1049`). `deck-view.js:2085` fetches `log.jsonl` with no bust.
- `branch.html` is at `?v=216`; the other four pages at 215. No test asserts
  cross-page agreement; `workbench.html` and `branch.html` have no bust test.
- `mana-map.css` uses **zero** design tokens (291 hard-coded hex);
  `tokens.css` has 24 tokens, 101 hard-coded hex beside them, 3 tokens never
  used, and a dead `details.case`/`.casefile` block (:138-158) superseded by
  :1002. Dead in both sheets: `.log-all`, `.log-last`, `.pl-sup`, `.lifts`,
  `.shell-empty`.
- 59 inline `onclick="MM.x()"` strings across seven files.
- `spaces.html` has no browser test; `deck.html` has ~3 direct browser tests
  (index.html has ~172).
- Read by the frontend, absent from `docs/data-artifacts.md`: the cardbert
  quartet, `eval/space_projections.json`, `data/reference/<slug>.json`,
  `diagnostic.json`, `goldfish_metrics/targets.json`, `log_annotations.json`,
  `bracket_report.json`, `build_plan.json`, `threat/targeting.json`,
  `tutor_guide.json`.

### J. Tests — the daily loop

- **The cache keys on all of `src/`** (#28): `CODE = (SRC,)` at
  `test_pilot_artifact_freshness.py:50` and `test_pilot_manual_freshness.py:38`,
  `INPUTS = (SRC, …)` at `test_pilot_tracked_artifacts_validate.py:51` —
  **279 cases** (the issue counted 236) re-run on any source edit.
- **#48 confirmed**: `conftest.py:247` and `:264` read `config.cache`
  unguarded; `-p no:cacheprovider` fails ~497 `unchanged`-fixture cases with
  `AttributeError: 'Config' object has no attribute 'cache'`. The guard is one
  line too late.
- Freshness tests **rewrite tracked artifacts in place and restore from a
  backup** (`test_pilot_artifact_freshness.py:104-116`); a crash mid-rerun
  leaves the tree dirty.
- 34 loops over a possibly-empty collection with no `assert checked >= N`
  (`test_pilot_deck_audit.py:42,60,75,404`, `test_pilot_model_staleness.py:56`,
  `test_pilot_pending.py:162`, `test_pilot_scenario_facts.py:142`,
  `test_pilot_deck_manifest.py:75,90`, `test_viz_viewer.py:239`,
  `test_serve.py:23,381`, `test_sim_parse.py:172,366,485`, …). Three tests with
  no assertion (`test_preprocess.py:280`, `test_sim_progress_branch.py:53`,
  `test_sven_spend.py:104`).
- Hard-coded slugs: heliod in 29 files (96 uses), ur-dragon 29 (87), radagast
  15 (53, a broken-down deck), hapatra parametrised in
  `test_pilot_goldfish_mana.py:81` (broken down). `test_pilot_goldfish_combat.py:241`
  asserts the exact opted-in set of six slugs. #35's three still unfixed:
  `test_pilot_goldfish_colors.py:61`, `test_pilot_goldfish_drain_and_draw.py:328`,
  `test_pilot_net_change.py:662`.
- `requires_deck` (547 items) gates on a **tracked** file, so it never skips on
  a clone; `requires_branch` and ~69 ad-hoc `skipif`s are undocumented in
  testing.md (which lists five markers).
- #31 (memo survives monkeypatch): untouched; no slug-keyed memo exists — the
  likelier mechanism is 22 modules doing `from manamap.config import DECKS_DIR`
  by value, while `test_serve.py:498-507` patches three modules.
- Browser: 249 tests (docs 225/227/223); JS-dispatched `.click()` sites have
  doubled since #16 was filed (~22 in `evaluate` strings vs ~42 real); 91
  `wait_for_timeout` calls (docs say 49).
- Runtime claims contradict each other in four places (Makefile:70 "~20s";
  testing.md:4-7 "~2.5 min"; testing.md:35 "735 s"; CLAUDE.md "772 s") and all
  predate ~450 added cases.

### K. Docs drift — S each, M for PLAN.md

- `docs/README.md`: four files unindexed (`ur-dragon-fork.md`,
  `ur-dragon-refactor.md`, `naya-treasure-shell.md`,
  `ur-dragon-treasure.decklist.txt`); size column stale on nearly every row and
  mixes units (gotchas rows are bullet counts); "About 7,500 lines" vs ~12,000;
  calls `manual-v5-spec.md` DRAFT when its own header says SUPERSEDED
  2026-09-02.
- `PLAN.md` is 1,314 lines, mostly DONE history; its deck table lists 10 decks
  with states that are wrong for ur-dragon (locked v1.2.1, not "v1.0.1"),
  heliod/gishath (locked, not PLACEHOLDER), zur (broken-down, not "on the
  bench"); "zero prescription files" (two exist); "`supersedes` has no
  scaffolding" (`deck-state supersede` exists); stack 007 "queued" (passed).
- CLAUDE.md: "18 subcommands" (28); "FOUR pages" (five); layout comment omits
  **35 pilot modules** and ~25 others (all of `sim/{experiment,pods,power,
  progress,failure,forge_cards,pilot_quality,pod_behaviour}`, `metrics.py`,
  `console.py`, `spaces.py`, `sven/`, `ingest/edhrec.py`); says `issue_length`
  is imported by live code (false); still says the compact page "replaces" the
  magazine in future tense.
- `vision.md:87` "Two surfaces" then lists three and omits `branch.html`;
  :118-128 calls `build_page` legacy and, four lines later, the replacement.
  `vision.md:3` / `simulation.md:4` say "last revised 2026-08-22" over 09-10
  content.
- `pipeline.md:26/28` names routines `writer-prose`, `the-ten`, `issue-plan`
  that no longer exist; PLAN says "10 static cache routines" (12).
- Legacy words in live docs: `sideboard` (pilot.md, data-artifacts.md), "Short
  List" (viz.md, data-artifacts.md), "Deck Lens" and a Plotly gotcha section
  (viz.md:1404), kianne/kinnan as decks (pilot.md, simulation.md, viz.md).
- `docs/pilot.md:1505` says the magazine was "replaced by `manual-v5-spec.md`";
  it was replaced by `poh.py`.
- `data-artifacts.md` has no row for `data/pods/`, `engine_casts`,
  `span_vectors.npy`, the cardbert set.
- Archive candidates → `docs/history/`: the four unindexed ur-dragon/naya memos
  (08-26, "nothing applied"), `manual-v5-spec.md`, `agent-audit-2026-08-19.md`.
  `prd-2026-08.md` stays (27 `PRD-v1 §N` citations).

### L. Data and repo hygiene — S to M

- 30% of commits mix code and regenerated artifacts (255/841; the four largest
  are 30K–435K lines). Reviewable only by the commit body.
- zur-enchantress: 8 branch dirs and 15 versions on a deck broken down 09-10.
- heliod `archangel-v1` merged with `into_version_before: null` (known-issues §10).
- Edgar's five single-card patch branches measured within noise (9e9f6d91),
  "decide or delete".
- `manuals/` still holds the frozen magazine HTML for 9 decks beside `manuals/p/`.
- `Makefile`: `demo` runs `build-index` three times; `serve` labels
  `manuals/index.html` "issues"; `check` is a bare alias; comment says
  "exactly ONE" serial test (three).

### M. Machine coupling — S to document, M to loosen

- `.dck` files go to the **macOS-only** `~/Library/Application Support/Forge/decks/commander`
  (`config.py:1299-1301`); `default_jobs()` shells
  `sysctl -n hw.perflevel0.logicalcpu` (`forge.py:516`); bare `java` on PATH
  (`forge.py:543,1036`); Sven's keychain hint uses macOS `security`
  (`llm.py:96-99`); `.mcp.json` uses a relative `.venv/bin/python`.
- Model ids hard-coded at `llm.py:29,34`.

---

## 6. A grooming order (a recommendation, not a plan)

1. **Housekeeping, one commit each, hours total.** Close #27, #42, #43. Update
   known-issues §1, §4, §5, §9, §13. Fix README's index and sizes, move the six
   archive candidates to `docs/history/`, prune PLAN.md to what is open, align
   `branch.html`'s bust, fix CLAUDE.md's counts and layout comment (or replace
   the layout comment with a pointer to a generated module list).
2. **The daily loop: #28 + #48 + #31.** Key the freshness caches on the modules
   an artifact actually depends on; guard `config.cache`; give `conftest` an
   autouse memo clear. Then re-measure `make test` and put ONE figure in
   testing.md.
3. **The branch lifecycle cluster #45–#48 + #25**, while it is fresh: a branched
   measurement records its own sha on disk, `stage` refuses or amends a
   PROPOSED branch, `--reason` on `propose` is rejected or honoured.
4. **One home for three predicates**: `decklist_sha` (bytes, one function),
   `expand_copies` (replace the 24), and the artifact registry (six lists →
   one, read by `deck_status`, `regen`, `promote`, `assess`, `deck_info`,
   `serve`). Each is mechanical and each has a test that proves it by
   re-introducing the divergence.
5. **Delete the magazine (#4)** now that the dependency map is one edge:
   split `line_cards` + the manifest out of `build_index.py`, retarget
   `make manuals` and CI's determinism diff at `manuals/p/`, delete the eight
   modules, `manuals/*.html`, and their 13 test files.
6. **goldfish decomposition**, proven by a byte-identical fleet
   `goldfish_metrics.json` diff; then extend `test_metric_hygiene` to the
   `model_*` flags and `model_coverage.CHANNELS`.
7. **Error handling as a class**: replace `SystemExit` inside library functions
   with a typed error that `main` converts (the goldfish `run` callers are the
   motivating case), give the 21 unreasoned `except Exception` a reason or a
   narrower type (#23).
8. **Park**: the PRD phase issues (#5–#14), the embedding chain (#12 → #40 →
   #38), and the parallelism pair (#29, #30) until the PRD says otherwise. The
   experiment modules (§5.G) are either documented as a research appendix or
   deleted with their four tracked artifacts; they should not stay half-in.
